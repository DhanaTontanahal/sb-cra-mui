
import { createMuiTheme } from '@material-ui/core/styles';
import { green, purple, blue, lightBlue,red } from '@material-ui/core/colors';

const theme = createMuiTheme({
    palette: {
      primary: blue,
      secondary: green,
    },
  });

  export default theme;